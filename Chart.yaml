apiVersion: v2
appVersion: 2.1.0
description: Shared templates for the small manifests each app needs alongside its upstream chart - ExternalSecrets, Certificates, ConfigMaps, and the nginx TLS-termination + tailscale-expose pattern
maintainers:
- email: ads@tekn.group
  name: TEKN Team
name: helm-library-manifests
type: application
version: 2.1.0
